《Project: Memory.log》第二章任务看板

最后更新时间：
2026-03-28（合并风道起手步骤）

--------------------------------------------------
一、待做
--------------------------------------------------

当前规则：
1. 默认只允许看板顶部 1 个任务作为“唯一推荐动作”。
2. 如果出现 `进行中`，新 AI 默认优先接它，不要跳到别的任务。
3. 若任务已完成，但相关文档没回写，不得把它视为真正完成。
4. 若达到任何小节点，但未更新 `docs/AI持续开发/15_项目管理进度汇报.txt`，也不得视为真正完成闭环。
5. 若达到任何小节点，但未立刻做一次本地 git 提交，也不得视为真正完成闭环。
6. 若新功能只便于脚本读取、但没有最小人类可见反馈，也不得视为真正达到“可测”。

唯一推荐动作：
1. [P1-CONTENT-02] 继续收束风道教学段的正式节奏与演出

完成标准：
1. `ch2_04_wind_tutorial` 的空间、压迫与顺序继续收束，不只停留在路线标签层
2. 不破坏风道段回归、控制技提示回归、解锁导流和章节冒烟
3. 人工试玩时更容易自然读到“风道优先，护盾兜底”的节奏

后续待做：
1. [P1-CONTENT-07] 继续校验并压缩章节提示链中的重复表达

--------------------------------------------------
二、进行中
--------------------------------------------------

1. [P1-CONTENT-02] 继续收束风道教学段的正式节奏与演出

--------------------------------------------------
三、已完成
--------------------------------------------------

1. 第二章总设计文档整理完成
2. 第二章六种基础动作表整理完成
3. 第二章怪物设计表整理完成
4. 第二章前 10 分钟关卡编排表整理完成
5. 第二章白盒开发待办表整理完成
6. AI 持续开发文档目录建立完成
7. 旧第二章初步玩法文档已删除
8. [P0-STRUCT-01] 已建立 `chapter2` 第一版目录骨架、`ch2_main.tscn` 与两个 section 骨架
9. [P0-TEST-01] 已建立 `scenes/tests/`、`scripts/tests/`、最小日志脚手架与 `test_runner.gd`
10. [P0-TEST-01] TC-01 `test_layer_switch.tscn` 与 TC-02 `test_bullet_to_wind.tscn` 已可执行并通过
11. [P0-CONTENT-01] 已把前三个关键阶段迁入 chapter2：`run_intro`、`wrong_layer_room`、`unlock_arena`
12. [P0-CONTENT-01] 已新增可复用 `ch2_unlock_trigger` setpiece，并确认 `ch2_main` 加载通过
13. [P0-STRUCT-02] 已补齐 `ch2_04`、`ch2_05`、`ch2_06`，chapter2 现具备 6 个 section 打包单元
14. [P0-TEST-05] `test_arena_flow.tscn` 已建立并通过，TC-04 已可执行
15. [P0-MECH-01] A 层“踢怪 / 强制挂起到 B 层”已落地，`test_kick_to_b.tscn` 通过
16. [P0-MECH-02] B 层普通锤击 / 组合切技已拆分，`test_hammer_return_single.tscn` 与 `test_layer_switch.tscn` 已覆盖关键规则
17. [P0-ENEMY-01] `Page-Lock Guard` 已独立实现并接入 chapter2，`test_page_lock_guard.tscn` 通过
18. [P0-ENEMY-02] `Front Hound` 已独立实现并接入 chapter2，`test_front_hound.tscn` 通过
19. [P0-TEST-08] `test_chapter2_first10_smoke.tscn` 已建立并通过
20. [P1-CONTENT-01] 第一场综合战白盒已接入 `ch2_06_first_mixed_arena`，`test_mixed_arena_flow.tscn` 通过
21. [P0-FLOW-01] 权限解锁流程已补最小导流闭环，`test_unlock_route_flow.tscn` 通过
22. [P0-DOCS-01] 已补充“小节点完成后立即本地 git 提交，commit message 需带类型关键字”的开发规则
23. [P1-FEEDBACK-02-A] 已为单体组合切技补屏幕规则提示、chapter2 教学文案与 `test_return_combo_tutorial.tscn`
24. [P1-MECH-01] 已实现 B 层重击切层群体带回原型，`heavy_attack` 单独按不切层，`heavy_attack + 切层键` 触发群体带回与最小 AOE，`test_hammer_return_group.tscn` 通过
25. [P1-CONTENT-03] 已把群体切层提示接入 `ch2_06_first_mixed_arena`，并把 `test_mixed_arena_flow.tscn` 扩展到校验新提示
26. [P0-DOCS-02] 已补充“双重可测”规则：功能既要便于 AI 自动测试，也要便于人类上手测试和直观看懂
27. [P1-MECH-02] 已实现 A 层控制技切层护盾原型，`control_skill` 单按不切层，`control_skill + 切层键` 切入 B 层并生成短时护盾，`test_control_shield.tscn` 通过
28. [P1-MECH-03] 已实现 A 层炸弹切层异化原型，`bomb_skill` 单按不切层，`bomb_skill + 切层键` 切入 B 层并触发异化爆发与短时危险区标记，`test_bomb_mutation.tscn` 通过
29. [P1-ENEMY-01] 已实现功能怪样板 `Background Turret`，并替换 `ch2_06_first_mixed_arena` 第二波里的旧功能位占位，`test_background_turret.tscn` 通过
30. [P1-TEST-09] 已建立 `test_mixed_arena_combo_regression.tscn`，第一次综合战现在已具备对白功能怪、群体切层、控制技护盾与炸弹异化入口的组合回归
31. [P1-CONTENT-04] 已把控制技护盾提示接入 `ch2_04_wind_tutorial`，`test_control_shield_route_hint.tscn` 通过
32. [P1-CONTENT-05] 已把炸弹异化提示接入 `ch2_06_first_mixed_arena`，`test_bomb_mutation_mixed_hint.tscn` 通过
33. [P1-CONTENT-02-A] 已为 `ch2_04_wind_tutorial` 接入 `Background Turret` 压迫源，并补 `test_wind_tutorial_pressure_layout.tscn`
34. [P1-CONTENT-06-A] 已把 `ch2_03_unlock_arena` 的解锁说明同步到控制技护盾与炸弹异化，并保持 `test_unlock_route_flow.tscn` 通过
35. [P1-CONTENT-06-B] 已把 `ch2_05_return_combo_tutorial` 的承接提示补成章节能力提示链，并新增 `test_chapter2_ability_arc_hints.tscn` 回归解锁说明、组合切回教学、风道护盾提示与综合战异化提示
36. [P1-CONTENT-02-B] 已把 `ch2_04_wind_tutorial` 收束为“先开枪做风道 -> 借高差继续追 -> 压力太大再带护盾切入 B 层”的三步可见路线提示，并保持风道段回归通过
37. [P1-CONTENT-07-A] 已把 `ch2_03_unlock_arena` 的解锁说明压缩为“先只练枪 -> 风道，后续切层派生稍后再讲”，并保持导流回归和章节能力提示链回归通过
38. [P1-CONTENT-02-C] 已为 `ch2_04_wind_tutorial` 补中间预备台与对应路线提示，让“做风道 -> 上高差 -> 追到炮台点”的节奏更容易从场景本体读出来，并保持可达性与风道段回归通过
39. [P1-CONTENT-02-D] 已把 `ch2_04_wind_tutorial` 的前台旧占位怪替换为 `Front Hound`，让风道段形成“前台追击 + 后台炮台”的真实双压力，并保持风道段回归通过
40. [P1-CONTENT-02-E] 已把 `ch2_04_wind_tutorial` 的 `Front Hound` 前移、后台炮台后压并放慢首轮节奏，让双压力更接近“先追击、再显压、最后才护盾兜底”的顺序，并保持风道段回归通过
41. [P1-CONTENT-02-F] 已为 `ch2_04_wind_tutorial` 补可见“护盾兜底区”与对应标签，让护盾兜底时机不只靠文字，也能从场景本体直接看出来，并保持风道段回归通过
42. [P1-CONTENT-02-G] 已把 `ch2_04_wind_tutorial` 的护盾兜底区继续后移到主路线后段，并明确标成“退路”，让这条解法更不容易被误读成默认路线，并保持风道段回归通过
43. [P1-CONTENT-07-B] 已压缩 `ch2_04_wind_tutorial` 中 `Hint_WindPath` 与 `Hint_ControlShield` 的重复说明，让场景信号开始替代长段文字，并保持风道段回归通过
44. [P1-CONTENT-07-C] 已删去 `ch2_04_wind_tutorial` 的泛标题提示，并把入口导流压成一句短决策，进一步降低风道段的标签负担，并保持风道段回归通过
45. [P1-CONTENT-07-D] 已把 `ch2_04_wind_tutorial` 的 `Step 1` 与 `Step 1.5` 合并成单个起手步骤标签，进一步减少入口附近的标签密度，并保持风道段回归通过

--------------------------------------------------
四、阻塞
--------------------------------------------------

当前无硬阻塞

软阻塞记录：
1. 风道教学段虽然已有三步路线提示、中间预备台、双压力怪组、初步压力顺序、可见护盾兜底区和退路标记，并已继续压缩标签负担，但正式演出和空间节奏仍未完全收束
2. 章节提示链虽然已接通，但不同段落之间仍可能有文案重复

--------------------------------------------------
五、下一步唯一推荐动作
--------------------------------------------------

默认推荐：
先继续收束风道教学段的正式节奏与演出。

内容收束后，下一推荐动作是：
再继续校验并压缩章节提示链中的重复表达。

然后再推进：
更高阶综合战回归和章节内容细化。
