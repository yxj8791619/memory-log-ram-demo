《Project: Memory.log》代码地图

--------------------------------------------------
一、工程入口
--------------------------------------------------

Godot 项目配置：
project.godot

当前主场景：
test_room.tscn

场景控制脚本：
test_room.gd

--------------------------------------------------
二、玩家相关
--------------------------------------------------

玩家场景：
player_g.tscn

玩家主逻辑：
player_g.gd

重点能力：
1. A/B 层切换
2. 层间不同物理参数
3. 射击
4. 大锤动画
5. 风道 buff
6. 下跳穿透平台

需要重点阅读的函数 / 区域：
1. `toggle_dimension`
2. `shoot`
3. 风 buff 相关逻辑
4. 下跳相关逻辑
5. `_update_collision_masks`

--------------------------------------------------
三、投射物与跨层对象
--------------------------------------------------

子弹场景：
bullet.tscn

子弹逻辑：
bullet.gd

重点：
当前已存在 `on_dimension_switched` 逻辑，
会把 A 层子弹转成 B 层风道。

这部分是第二章 A -> B 非目标切层设计的现有落地点。

--------------------------------------------------
四、大锤命中相关
--------------------------------------------------

大锤命中逻辑：
hammer_hitbox.gd

注意：
当前该文件仍带有明显调试打印，属于未收束状态。
如果要实现 B -> A 切层带怪，大概率要重写或显著整理这里的处理逻辑。

--------------------------------------------------
五、现有敌人
--------------------------------------------------

普通敌人：
enemy_dummy.gd
enemy_dummy.tscn

护盾敌人：
enemy_shield.gd
enemy_shield.tscn

用途：
这些敌人可以作为旧白盒参考，但不应被视为第二章新体系的最终样板。

--------------------------------------------------
六、关卡与白盒组件
--------------------------------------------------

竞技场触发器：
arena_trigger.gd
arena_trigger.tscn

缓存碎片：
cache_fragment.gd
cache_fragment.tscn

单向平台：
one_way_platform.tscn

瓦片集：
main_tile_set.tres

--------------------------------------------------
七、最可能被新 AI 修改的文件
--------------------------------------------------

如果下一轮开始做实现，最有可能改动的是：

1. test_room.tscn
2. test_room.gd
3. player_g.gd
4. player_g.tscn
5. hammer_hitbox.gd
6. 新增的敌人脚本与场景

--------------------------------------------------
八、推荐阅读顺序
--------------------------------------------------

如果你准备开始写代码，建议按以下顺序读代码：

1. player_g.gd
2. player_g.tscn
3. bullet.gd
4. hammer_hitbox.gd
5. test_room.tscn
6. test_room.gd
7. 现有敌人脚本

补充阅读建议：
如果任务涉及叙事、命名、系统语义、角色动机或 UI 文案，应补读：
1. docs/核心概念设计文档（GDD）-3.0.docx
2. docs/设定集-至第一章.docx
3. docs/游戏角色设定-CORE.docx
4. docs/游戏角色设定-小黑.docx

--------------------------------------------------
九、代码实现注意事项
--------------------------------------------------

1. 现有工程仍处于白盒阶段，不必过早为所有系统做过度抽象
2. 但 B -> A 带怪逻辑应尽量做成可复用结构，不要写成一次性特判
3. 任何新增怪物都应优先服务第二章样板验证，而不是追求完整终版 AI
